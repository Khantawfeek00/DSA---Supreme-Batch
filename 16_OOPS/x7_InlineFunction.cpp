#include <bits/stdc++.h>
using namespace std;

inline void show(int num)
{
    cout << num << endl;
}
// inline function
int main()
{
    show(10);
    show(10);
    show(10);
    show(10);
    show(10);
    show(10);
    show(10);
    show(10);
    show(10);
    show(10);
    show(10);
}